import test from 'node:test';import assert from 'node:assert/strict';import {achievements,emptyState,validateState,safeUrl,progress} from '../dist/model.js';
test('export round trip preserves manual confirmation independently of counts',()=>{const s=emptyState();s.entries['pull-shark']={count:2,confirmed:false,evidence:['https://github.com/brokenasf/example/pull/1']};assert.deepEqual(validateState(JSON.parse(JSON.stringify(s))),s);});
test('all tier boundaries and next goals',()=>{for(const a of achievements){for(const [i,t] of a.tiers.entries()){assert.equal(progress(a,t-1).level,i);assert.equal(progress(a,t).level,i+1);}assert.equal(progress(a,a.tiers.at(-1)).remaining,0);assert.equal(progress(a,0).percent,0);}});
test('unsafe links and malformed imports rejected without mutating input',()=>{for(const url of ['javascript:alert(1)','https://github.com.evil.test/a','https://user:pass@github.com/a','http://github.com/a'])assert.throws(()=>safeUrl(url));for(const count of [-1,1.5,Infinity,'2',100000001]){const s=emptyState();s.entries.yolo.count=count;assert.throws(()=>validateState(s));}assert.throws(()=>validateState({version:2,entries:{}}));const s=emptyState();s.entries.yolo.evidence=['https://example.com'];assert.throws(()=>validateState(s));assert.deepEqual(s.entries.yolo.evidence,['https://example.com']);});
